# Banco de dados (Turso / libsql) — usado em produção (Vercel).
# Deixe em branco localmente para usar o arquivo prisma/dev.db.
TURSO_DATABASE_URL=
TURSO_AUTH_TOKEN=

# Segredo usado para assinar os tokens JWT de login.
# Gere um valor aleatório (ex.: `openssl rand -base64 32`).
NEXTAUTH_SECRET=
